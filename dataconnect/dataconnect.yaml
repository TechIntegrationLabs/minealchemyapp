specVersion: "v1beta"
serviceId: "minealchemyapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "minealchemyapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
